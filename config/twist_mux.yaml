twist_mux:
  ros__parameters:
    use_stamped: true    # ← add this line
    topics:
      joystick:
        topic: /cmd_vel_joy
        timeout: 0.5
        priority: 100
      keyboard:
        topic: /cmd_vel_key
        timeout: 0.5
        priority: 50
      navigation:
        topic: /cmd_vel_nav
        timeout: 0.5
        priority: 10